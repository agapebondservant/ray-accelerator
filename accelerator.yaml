accelerator:
  displayName: Bitnami KubeRay Accelerator
  description: Template for deploying Bitnami Kuberay to TAP
  iconUrl: https://static-00.iconduck.com/assets.00/airflow-icon-512x512-tpr318yf.png
  tags:
    - "ML"
    - "AI"
    - "datascience"
    - "MLOps"
    - "ray"
  options:
    - name: ray
      inputType: radio
      display: true
      choices:
        - text: 'Ray (ML Training)'
          value: selected
      defaultValue: selected
      dependsOn:
        name: mlPlatformType
        value: training
    - name: mlPlatformType
      inputType: radio
      display: true
      choices:
        - text: 'View guidance manual: https://github.com/bitnami/charts/tree/main/bitnami/airflow'
          value: training
      defaultValue: training
    - name: rayImageTag
      inputType: text
      display: true
      label: 'rayImage.tag'
      defaultValue: '2.9.3-debian-12-r0'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: operatorImageTag
      inputType: text
      display: true
      label: 'operator.image.tag'
      defaultValue: '1.0.0-debian-12-r15'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: apiserverImageTag
      inputType: text
      display: true
      label: 'apiserver.image.tag'
      defaultValue: '1.0.0-debian-12-r12'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: clusterHeadResourcesLimitsMemory
      inputType: text
      display: true
      label: 'cluster.head.resources.limits.memory'
      defaultValue: '8G'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: clusterHeadResourcesRequestsMemory
      inputType: text
      display: true
      label: 'cluster.head.resources.requests.memory'
      defaultValue: '8G'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: clusterWorkerCommonResourcesLimitsMemory
      inputType: text
      display: true
      label: 'cluster.worker.common.resources.limits.memory'
      defaultValue: '8G'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: clusterWorkerCommonResourcesRequestsMemory
      inputType: text
      display: true
      label: 'cluster.worker.common.resources.requests.memory'
      defaultValue: '8G'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: operatorServiceType
      inputType: text
      display: true
      label: 'operator.service.type'
      defaultValue: 'LoadBalancer'
      dependsOn:
        name: mlPlatformType
        value: training
    - name: apiserverServiceType
      inputType: text
      display: true
      label: 'apiserver.service.type'
      defaultValue: 'LoadBalancer'
      dependsOn:
        name: mlPlatformType
        value: training
engine:
  type: Merge
  sources:
    - type: Chain
      transformations:
        - type: Include
          patterns: [ 'resources/values-overrides.yaml' ]
        - type: ReplaceText
          substitutions:
            - text: REPLACE_RAY_IMAGE_TAG
              with: "#rayImageTag"
            - text: REPLACE_OPERATOR_IMAGE_TAG
              with: "#operatorImageTag"
            - text: REPLACE_APISERVER_IMAGE_TAG
              with: "#apiserverImageTag"
            - text: REPLACE_CLUSTER_HEAD_RESOURCES_LIMITS_MEMORY
              with: "#clusterHeadResourcesLimitsMemory"
            - text: REPLACE_CLUSTER_HEAD_RESOURCES_REQUESTS_MEMORY
              with: "#clusterHeadResourcesRequestsMemory"
            - text: REPLACE_CLUSTER_WORKER_COMMON_RESOURCES_LIMITS_MEMORY
              with: "#clusterWorkerCommonResourcesLimitsMemory"
            - text: REPLACE_CLUSTER_WORKER_COMMON_RESOURCES_REQUESTS_MEMORY
              with: "#clusterWorkerCommonResourcesRequestsMemory"
            - text: REPLACE_OPERATOR_SERVICE_TYPE
              with: "#operatorServiceType"
            - text: REPLACE_API_SERVER_SERVICE_TYPE
              with: "#apiserverServiceType"

